const { Router } = require("express");
const router = Router();
const { User } = require("../db");
const { passport } = require8("passport-local");

router.post("/login", (req, res) => {});

module.exports = router;
